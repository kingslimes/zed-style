{
  "name": "zed-style",
  "module": "src/index.tsx",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "csstype": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "build:jsx": "bun run build.ts",
    "build:type": "tsc -p tsconfig.types.json",
    "export": "bun run build:jsx && bun run build:type"
  },
  "dependencies": {
    "autoprefixer": "^10.4.23",
    "postcss": "^8.5.6"
  }
}
